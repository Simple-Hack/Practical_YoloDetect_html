{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "go: build package",
            "type": "shell",
            "command": "D:\\Code-Space\\Go\\bin\\go.exe",
            "args": [
                "build",
                "-o",
                "${fileDirname}\\${fileBasenameNoExtension}.exe",
                "${fileDirname}"
            ],
            "problemMatcher": [
                "$go"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Builds the current Go package in the specified directory."
        }
    ]
}